# typescript-data-fetching-tasks.md
# Data Fetching — Tarefas de Correção

Gerado por: `/typescript:data-fetching`
Workspace: `output/workspace/system-forge-landing-page`
Data: 2026-03-25

---

## Tarefas

| # | Arquivo | Linha | Severidade | Categoria | Problema | Correção proposta | Dependência |
|---|---------|-------|------------|-----------|----------|-------------------|-------------|
| T-001 | `src/components/ui/NewsletterOptIn.tsx` | 67–107 | **HIGH** | Duplicação de client | O componente reimplementa manualmente toda a lógica de fetch da newsletter (`fetch`, `AbortController`, timeout, tratamento de erro) em vez de usar `subscribeNewsletter()` de `src/lib/services/newsletter.ts`. As duas implementações podem divergir silenciosamente. | Substituir o bloco try/catch do `handleSubmit` por uma chamada a `subscribeNewsletter({ email, consent, website })`. Mapear `NETWORK_ERROR` / `TIMEOUT` / `NEWSLETTER_NOT_CONFIGURED` para os toasts correspondentes. | — |
| T-002 | `src/hooks/useArticleSearch.ts` | 34–75 | **MEDIUM** | Cancelamento / leak | O `fetch('/search-index.json')` dentro de `useEffect` não usa `AbortController`. Se a query mudar enquanto o carregamento está em curso, ou o componente desmontar, a promise continua e pode chamar `setIsLoading(false)` em estado stale. | Criar um `AbortController` no início do `useEffect`; passar `signal` ao `fetch`; no cleanup `return () => controller.abort()`. Checar `err.name === 'AbortError'` no catch para não marcar `hasError`. | — |
| T-003 | `src/hooks/useArticleSearch.ts` | 40, 60, 65–70 | **MEDIUM** | AsyncState / UX | `setIsLoading(false)` é chamado apenas no bloco `if (!fuseRef.current)` (linha 60). Se ocorrer um erro durante o carregamento, `isLoading` fica `true` para sempre (spinner infinito). | Mover `setIsLoading(false)` para o bloco `finally` do `search()`, garantindo reset em qualquer caminho de erro. | T-002 |
| T-004 | `src/hooks/useArticleSearch.ts` | 45 | **LOW** | Validação de schema | O payload de `/search-index.json` é cast com `as Promise<SearchIndexItem[]>` sem validação runtime. Se o JSON mudar (campo renomeado, campo ausente), erros de runtime surgem sem mensagem útil. | Usar `z.array(SearchIndexItemSchema).parse(await r.json())` com um schema Zod mínimo para `SearchIndexItem`. Alternativa aceitável: `safeParse` com fallback e log de aviso. | — |
| T-005 | `cloudflare-worker/newsletter-proxy/index.ts` | 133, 184, 215, 249, 258 | **MEDIUM** | Timeout / resiliência | Nenhuma das chamadas `fetch()` ao `api.resend.com` usa `AbortController` / timeout. O Worker pode ficar suspenso até 30 s em caso de lentidão do Resend, consumindo o budget de CPU do Cloudflare. | Envolver cada fetch crítico com um helper `fetchWithTimeout(url, init, ms=8000)` usando `AbortController`. Para as chamadas fire-and-forget (NOTIF-002, NOTIF-003), usar `ctx.waitUntil(fetchWithTimeout(...))` para não bloquear a resposta. | — |
| T-006 | `cloudflare-worker/newsletter-proxy/index.ts` | 215–227 | **MEDIUM** | Silêncio em erro | O envio do email de boas-vindas (NOTIF-002) em `handleConfirmation` é `await`ed mas o resultado não é verificado (`!resendResponse.ok`). Falha silenciosa: usuário é redirecionado para a página de sucesso mas nunca recebe o email de boas-vindas. | Checar `resendResponse.ok`; em caso de falha, logar o erro (`console.error`) e redirecionar com `?error=welcome_email_failed` para que o Next.js exiba mensagem adequada. | T-005 |
| T-007 | `cloudflare-worker/newsletter-proxy/index.ts` | 249–255 | **MEDIUM** | Silêncio em erro | O `DELETE` de contato na Resend audience em `handleUnsubscribe` não verifica o status da resposta. Se o Resend retornar 404 ou 500, o token é deletado e o usuário recebe confirmação de cancelamento, mas o contato ainda existe na audience. | Verificar `deleteResponse.ok`; se `!ok`, retornar `400 / 500` com mensagem de erro em vez de deletar o token e fingir sucesso. | T-005 |
| T-008 | `cloudflare-worker/newsletter-proxy/index.ts` | 184–200 | **LOW** | Idempotência | `POST /audiences/{id}/contacts` com email duplicado retorna 400 na Resend API. Não há tratamento para reconfirmação: se o usuário clicar no link de confirmação duas vezes (janela de 48 h), o segundo clique resulta em `?error=activation_failed` sem diagnóstico claro. | Verificar se o status 400 da Resend é especificamente "contact already exists" (código da API) e redirecionar com `?success=true` para tratar como idempotente, ou com `?error=already_confirmed` para UX mais clara. | — |
