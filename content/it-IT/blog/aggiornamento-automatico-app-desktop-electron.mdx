---
title: "Auto-update nelle app desktop: Electron Forge"
slug: "aggiornamento-automatico-app-desktop-electron"
date: "2024-09-28"
description: "Come implementare l'auto-update nelle app Electron con electron-updater e GitHub Releases — release channel, staging, rollback e notifica all'utente."
tags: ["electron", "auto-update", "electron-updater", "desktop app", "ci cd"]
coverImage: "/images/blog/default-cover.png"
author: "Pedro Corgnati"
relatedService: "desktop"
locale: 'it-IT'
published: true
exclusive: false
excerpt: Come implementare l'auto-update nelle app Electron con electron-updater e GitHub Releases — release channel, staging, rollback e notifica all'utente.
hreflang_pair:
  - locale: 'pt-BR'
    slug: 'auto-update-apps-desktop-electron-forge'
  - locale: 'it-IT'
    slug: 'aggiornamento-automatico-app-desktop-electron'
  - locale: 'en'
    slug: 'auto-update-desktop-apps-electron-forge'
---

Distribuire un'app desktop senza un meccanismo di auto-update significa firmare un contratto con il caos futuro. In sei mesi ti ritroverai con utenti su cinque versioni diverse dell'app, ognuna con bug diversi, comportamenti diversi, che chiedono supporto per problemi risolti settimane fa. Tenere traccia di quale versione usa ogni cliente e convincerli ad aggiornarsi manualmente è un lavoro che non dovrebbe esistere.

L'auto-update ben implementato elimina questo problema alla radice. L'utente installa una volta. Dopodiché, l'app si aggiorna silenziosamente in background — o con una notifica discreta quando è disponibile un aggiornamento. La base utenti converge alla versione più recente in giorni, non in mesi.

La buona notizia è che l'ecosistema Electron ha una soluzione matura per questo: `electron-updater`, che funziona con GitHub Releases come server di distribuzione. La cattiva notizia è che un'implementazione corretta — con canali di release, rollout graduale e strategia di rollback — richiede più attenzione di quanto suggerisca l'esempio base della documentazione.

## electron-updater: Setup di Base con GitHub Releases

`electron-updater` fa parte del pacchetto `electron-builder` ed è la soluzione più usata per l'auto-update nelle app Electron. Verifica periodicamente se è disponibile una nuova versione su un server di aggiornamento — che può essere GitHub Releases, S3, o un server proprietario.

Il setup iniziale richiede configurazione nel `package.json` e nel main process:

```javascript
// main.js — configurazione dell'auto-updater
const { autoUpdater } = require('electron-updater')
const { ipcMain, dialog } = require('electron')
const log = require('electron-log')

// Configurare il logging per la diagnostica
autoUpdater.logger = log
autoUpdater.logger.transports.file.level = 'info'

// Disabilitare il download automatico per controllare quando installare
autoUpdater.autoDownload = false

function setupAutoUpdater(mainWindow) {
  // Controllare gli aggiornamenti all'avvio (con ritardo di 3 secondi)
  setTimeout(() => {
    autoUpdater.checkForUpdates()
  }, 3000)

  // Controllare nuovamente ogni 4 ore
  setInterval(() => {
    autoUpdater.checkForUpdates()
  }, 4 * 60 * 60 * 1000)

  autoUpdater.on('update-available', (info) => {
    // Notificare il renderer process
    mainWindow.webContents.send('update-available', info)
  })

  autoUpdater.on('download-progress', (progress) => {
    mainWindow.webContents.send('download-progress', progress)
  })

  autoUpdater.on('update-downloaded', (info) => {
    mainWindow.webContents.send('update-downloaded', info)
  })

  // Avviare il download quando l'utente conferma
  ipcMain.on('start-download', () => {
    autoUpdater.downloadUpdate()
  })

  // Installare e riavviare quando l'utente conferma
  ipcMain.on('install-update', () => {
    autoUpdater.quitAndInstall(false, true)
  })
}

module.exports = { setupAutoUpdater }
```

La configurazione nel `package.json` definisce il server di pubblicazione:

```json
{
  "build": {
    "publish": {
      "provider": "github",
      "owner": "your-username",
      "repo": "your-repository",
      "private": false
    }
  }
}
```

Con questa configurazione, pubblicando un GitHub Release con gli artefatti di build, `electron-updater` rileva automaticamente la nuova versione e avvia il processo di aggiornamento.

## Release Channel: Stable, Beta e Nightly

Un singolo canale di release non è sufficiente per la maggior parte dei progetti seri. La pratica standard del settore è avere almeno due canali: `stable` per la base utenti in produzione, e `beta` per i tester che accettano versioni non finalizzate in cambio di accesso anticipato alle novità.

`electron-updater` supporta i canali nativamente. La versione nel `package.json` definisce il canale:

- `1.0.0` — versione stable
- `1.1.0-beta.1` — versione beta
- `1.1.0-alpha.1` — versione alpha/nightly

Nel codice, il canale può essere configurato dinamicamente in base a una preferenza dell'utente:

```javascript
// Leggere la preferenza del canale salvata nelle impostazioni
const channel = store.get('updateChannel', 'stable')
autoUpdater.channel = channel

// Nell'UI delle impostazioni, permettere all'utente di cambiare canale
ipcMain.on('set-update-channel', (event, newChannel) => {
  store.set('updateChannel', newChannel)
  autoUpdater.channel = newChannel
  autoUpdater.checkForUpdates()
})
```

La comunicazione del canale con il pubblico giusto è importante: gli utenti che hanno optato per il canale beta sanno che possono ricevere versioni con bug occasionali. Questo segmenta il feedback e permette di identificare i problemi prima che influenzino tutta la base utenti.

## Staging degli Aggiornamenti: Rollout Graduale per Percentuale

Per applicazioni con una base utenti ampia, lanciare una versione per tutti simultaneamente è rischioso. Un bug critico che supera i test influenzerà tutti gli utenti allo stesso tempo.

Il rollout graduale — lanciare prima al 5% degli utenti, osservare le metriche per 24 ore, poi espandere al 20%, 50% e 100% — è una pratica standard per mitigare questo rischio.

`electron-updater` non ha supporto nativo al rollout percentuale, ma è possibile implementarlo con logica nel server o con un livello di feature flag:

```javascript
// Strategia: verificare il flag di rollout prima di avviare l'aggiornamento
autoUpdater.on('update-available', async (info) => {
  // Generare un identificatore univoco stabile per l'istanza
  const instanceId = getOrCreateInstanceId()

  // Consultare il server per verificare se questa istanza è nel rollout
  try {
    const response = await fetch(
      `https://api.yourapp.com/update-eligibility?version=${info.version}&instanceId=${instanceId}`
    )
    const { eligible } = await response.json()

    if (eligible) {
      mainWindow.webContents.send('update-available', info)
    }
  } catch {
    // In caso di fallimento nella verifica, non bloccare l'aggiornamento
    mainWindow.webContents.send('update-available', info)
  }
})
```

Il server controlla la percentuale di istanze eleggibili per ogni versione, permettendo di aumentare gradualmente il rollout man mano che la fiducia nella versione cresce.

## Rollback: Come Tornare a una Versione Precedente

Nessun processo di QA è perfetto. Prima o poi una versione problematica raggiungerà gli utenti. Avere un meccanismo di rollback — automatico o manuale — è la differenza tra un incidente controllato e una crisi di supporto.

La strategia più semplice è mantenere la versione precedente disponibile su GitHub Releases e reindirizzare il server di aggiornamenti verso di essa quando necessario. Se il `latest.yml` (file che `electron-updater` consulta) punta alla versione precedente, le istanze che non si sono ancora aggiornate non riceveranno la versione problematica.

Per gli utenti che hanno già installato la versione con problemi, l'approccio più efficace è pubblicare una nuova versione corretta — anche se è un hotfix minimo — il più rapidamente possibile. L'auto-update poi sostituisce la versione problematica con quella corretta.

Nei casi critici, dove l'app crasha all'avvio e l'auto-update non riesce a essere eseguito, è necessario avere una procedura manuale documentata. Questo include mantenere i programmi di installazione di versioni precedenti disponibili per il download diretto e avere un canale di comunicazione con gli utenti colpiti.

Il punto più importante sul rollback è: pianificalo prima di averne bisogno. Quando c'è un incidente in produzione non è il momento di scoprire che il processo di rollback non è stato testato.

## Conclusione

L'auto-update ben implementato non è un dettaglio di distribuzione — è parte dell'esperienza del prodotto. Gli utenti che non devono mai preoccuparsi di aggiornare manualmente l'app hanno meno attrito nel quotidiano. I team di sviluppo che possono rilasciare aggiornamenti con fiducia, sapendo di avere canali di staging e rollback, costruiscono con più agilità.

La complessità reale sta nei dettagli: firmare correttamente i programmi di installazione (senza firma del codice Windows blocca gli aggiornamenti), gestire gli utenti che lasciano il computer acceso senza riavviare, comunicare aggiornamenti critici di sicurezza in modo urgente ma non invasivo.

In SystemForge, l'auto-update con channel, staged rollout e capacità di rollback fa parte del baseline di qualsiasi app desktop che costruiamo. Se stai sviluppando un'app Electron e vuoi implementare una strategia di aggiornamento robusta fin dall'inizio, possiamo aiutarti a strutturarla correttamente.
