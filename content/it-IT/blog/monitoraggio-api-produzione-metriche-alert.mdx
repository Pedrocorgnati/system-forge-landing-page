---
title: "Monitoraggio delle API in produzione: metriche e alert"
slug: "monitoraggio-api-produzione-metriche-alert"
date: "2024-11-22"
description: "Come monitorare le API in produzione — latenza p50/p95/p99, error rate, alert con PagerDuty, distributed tracing con OpenTelemetry e dashboard Grafana."
tags: ["monitoraggio api", "opentelemetry", "grafana", "alert", "osservabilità"]
coverImage: "/images/blog/default-cover.png"
author: "Pedro Corgnati"
relatedService: "api-integracoes"
locale: 'it-IT'
published: true
exclusive: false
excerpt: Come monitorare le API in produzione — latenza p50/p95/p99, error rate, alert con PagerDuty, distributed tracing con OpenTelemetry e dashboard Grafana.
hreflang_pair:
  - locale: 'pt-BR'
    slug: 'monitoramento-apis-producao-metricas-alertas'
  - locale: 'it-IT'
    slug: 'monitoraggio-api-produzione-metriche-alert'
  - locale: 'en'
    slug: 'api-monitoring-in-production-metrics-alerts'
---

Esistono due modi per scoprire che la tua API ha problemi in produzione: i tuoi alert te lo comunicano, o i tuoi clienti te lo comunicano. Il primo è solo un incidente. Il secondo è un incidente e una crisi di fiducia.

Il monitoraggio dell'API in produzione non è opzionale — è l'infrastruttura di qualità che distingue un servizio professionale da un prototipo in produzione. E con gli strumenti giusti, configurare un'osservabilità di base richiede meno di una giornata di lavoro.

## Metriche Essenziali: Latenza, Error Rate e Throughput

Le tre metriche fondamentali di qualsiasi API sono conosciute come i "Golden Signals" dell'SRE (Site Reliability Engineering):

**Latenza** — quanto tempo impiega la tua API a rispondere. L'errore classico è monitorare solo la media. La media nasconde gli outlier: se il 95% delle richieste impiega 50ms e il 5% impiega 10 secondi, la media può essere 550ms — che sembra ragionevole ma rappresenta una pessima esperienza per 1 utente su 20.

I percentili corretti da monitorare:
- **p50 (mediana):** L'utente "mediano". Buona baseline di performance.
- **p95:** Il 95% delle richieste è sotto questo valore. Ciò che sperimenta la maggior parte degli utenti.
- **p99:** La "coda lunga". Dove si nascondono i veri problemi di performance.
- **p99.9:** Per SLA critici. Un'API con p99.9 di 2 secondi ha lo 0,1% delle richieste più lente di quel valore.

**Error Rate** — percentuale di richieste che restituiscono 5xx. Un picco improvviso dallo 0% al 5% di error rate è più significativo di un error rate costante al 2%.

**Throughput** — quante richieste al secondo. Utile per rilevare sia cali anomali di traffico che picchi che possono indicare abusi.

```typescript
// Strumentazione manuale con metriche custom in Next.js
import { metrics } from "@opentelemetry/api";

const meter = metrics.getMeter("api-metrics");

const httpRequestDuration = meter.createHistogram("http_request_duration_ms", {
  description: "Durata delle richieste HTTP in millisecondi",
  unit: "ms",
});

const httpRequestsTotal = meter.createCounter("http_requests_total", {
  description: "Totale richieste HTTP",
});

// Middleware di strumentazione
export function instrumentRequest(handler: NextApiHandler): NextApiHandler {
  return async (req, res) => {
    const startTime = Date.now();
    const labels = {
      method: req.method ?? "unknown",
      route: req.url?.split("?")[0] ?? "unknown",
    };

    try {
      await handler(req, res);
      const duration = Date.now() - startTime;
      httpRequestDuration.record(duration, { ...labels, status: String(res.statusCode) });
      httpRequestsTotal.add(1, { ...labels, status: String(res.statusCode) });
    } catch (error) {
      httpRequestDuration.record(Date.now() - startTime, { ...labels, status: "500" });
      httpRequestsTotal.add(1, { ...labels, status: "500" });
      throw error;
    }
  };
}
```

## OpenTelemetry: Distributed Tracing nella Pratica

Le metriche dicono "qualcosa non va". I trace dicono "esattamente dove non va e perché". OpenTelemetry (OTel) è lo standard aperto per la strumentazione dell'osservabilità — funziona con qualsiasi backend (Jaeger, Tempo, Datadog, New Relic, Honeycomb).

```typescript
// Configurazione di OpenTelemetry in Next.js (instrumentation.ts)
import { NodeSDK } from "@opentelemetry/sdk-node";
import { OTLPTraceExporter } from "@opentelemetry/exporter-trace-otlp-http";
import { OTLPMetricExporter } from "@opentelemetry/exporter-metrics-otlp-http";
import { PeriodicExportingMetricReader } from "@opentelemetry/sdk-metrics";
import { getNodeAutoInstrumentations } from "@opentelemetry/auto-instrumentations-node";

export function register() {
  const sdk = new NodeSDK({
    traceExporter: new OTLPTraceExporter({
      url: process.env.OTEL_EXPORTER_OTLP_ENDPOINT ?? "http://localhost:4318/v1/traces",
    }),
    metricReader: new PeriodicExportingMetricReader({
      exporter: new OTLPMetricExporter({
        url: process.env.OTEL_EXPORTER_OTLP_ENDPOINT ?? "http://localhost:4318/v1/metrics",
      }),
    }),
    instrumentations: [
      getNodeAutoInstrumentations({
        "@opentelemetry/instrumentation-fs": { enabled: false }, // troppo verboso
      }),
    ],
    serviceName: "api-ordini",
  });

  sdk.start();
}
```

Con le auto-strumentazioni, ottieni gratuitamente trace di: richieste HTTP, query Prisma/TypeORM, comandi Redis, chiamate fetch/axios e messaggi di coda.

## Grafana + Prometheus: Dashboard API in 30 Minuti

```yaml
# docker-compose.yml — stack di osservabilità locale
version: "3.8"
services:
  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana-data:/var/lib/grafana

  tempo:
    image: grafana/tempo:latest
    ports:
      - "4317:4317"   # OTLP gRPC
      - "4318:4318"   # OTLP HTTP
      - "3200:3200"   # Tempo API

volumes:
  grafana-data:
```

Query Prometheus essenziali per dashboard API:

```promql
# Latenza p99 per route (ultimi 5 minuti)
histogram_quantile(0.99,
  rate(http_request_duration_ms_bucket[5m])
) by (route)

# Error rate per route
sum(rate(http_requests_total{status=~"5.."}[5m])) by (route)
/
sum(rate(http_requests_total[5m])) by (route)

# Throughput (req/s)
sum(rate(http_requests_total[1m])) by (route)
```

## Alert Intelligenti: Evitare l'Alert Fatigue

L'alert fatigue è il fenomeno in cui un team smette di rispondere agli alert perché ne riceve così tanti senza urgenza reale che quelli rilevanti si perdono nel rumore.

**Principi degli alert intelligenti:**

**Alert sui sintomi, non sulle cause.** "Error rate > 5%" è un sintomo: qualcosa non va per gli utenti. "CPU > 80%" è una causa: può o meno stare influenzando gli utenti.

**Burn rate, non soglie assolute.** Invece di alertare quando error rate > 1%, alerta quando stai "bruciando" il tuo SLO più velocemente del sostenibile.

```yaml
# Alert in Prometheus (alertmanager)
groups:
  - name: api-alerts
    rules:
      - alert: HighErrorBurnRate
        expr: |
          (
            sum(rate(http_requests_total{status=~"5.."}[1h]))
            / sum(rate(http_requests_total[1h]))
          ) > 0.05
        for: 5m
        labels:
          severity: critical
          team: backend
        annotations:
          summary: "Error rate sopra il 5% per 5 minuti"
          runbook: "https://wiki.azienda.com/runbooks/high-error-rate"

      - alert: HighP99Latency
        expr: |
          histogram_quantile(0.99,
            rate(http_request_duration_ms_bucket[5m])
          ) > 2000
        for: 3m
        labels:
          severity: warning
        annotations:
          summary: "P99 di latenza sopra i 2 secondi"
```

**Tre livelli di severità:**
- **Critical:** sveglia qualcuno adesso. SLO a rischio immediato, utenti impattati.
- **Warning:** indaga durante l'orario di lavoro. Tendenza preoccupante.
- **Info:** log per audit. Non interrompe nessuno.

## Conclusione

L'osservabilità dell'API non è un progetto futuro — è un requisito di produzione. La buona notizia è che il costo di ingresso è drasticamente diminuito: OpenTelemetry è gratuito e open source, Grafana + Prometheus può essere self-hosted con costo zero, e la configurazione di base richiede meno di una giornata di lavoro.

In SystemForge, il monitoraggio e gli SLO fanno parte della fase di deploy — non vengono aggiunti dopo come pensiero tardivo. Questo significa che ogni sistema consegnato arriva con dashboard funzionanti, alert configurati e runbook di base.
