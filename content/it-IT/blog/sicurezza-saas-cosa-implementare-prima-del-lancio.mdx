---
title: "Sicurezza SaaS: cosa implementare prima del lancio"
slug: "sicurezza-saas-cosa-implementare-prima-del-lancio"
date: "2024-10-30"
description: "Checklist di sicurezza obbligatoria per SaaS prima del lancio: autenticazione, autorizzazione, crittografia, rate limiting, audit e protezione dei dati."
tags: ["sicurezza saas", "autenticazione", "owasp", "crittografia", "compliance saas"]
coverImage: "/images/blog/default-cover.png"
author: "Pedro Corgnati"
relatedService: "saas"
locale: 'it-IT'
published: true
exclusive: false
excerpt: Checklist di sicurezza obbligatoria per SaaS prima del lancio: autenticazione, autorizzazione, crittografia, rate limiting, audit e protezione dei dati.
hreflang_pair:
  - locale: 'pt-BR'
    slug: 'seguranca-saas-o-que-implementar-antes-de-lancar'
  - locale: 'it-IT'
    slug: 'sicurezza-saas-cosa-implementare-prima-del-lancio'
  - locale: 'en'
    slug: 'saas-security-what-to-implement-before-launch'
---

Una falla di sicurezza subito dopo il lancio può chiudere un SaaS prima ancora che abbia la possibilità di dimostrare il suo valore. Non necessariamente perché l'attacco sia catastrofico in scala — ma perché la reputazione è il patrimonio più fragile di un prodotto nuovo. Un data breach dei clienti nel primo mese di operatività raramente è recuperabile.

La buona notizia è che la maggior parte delle vulnerabilità sfruttate nei nuovi SaaS non sono zero-day sofisticati. Sono falle di base e prevedibili: password senza limite di tentativi, token esposti nei log, assenza di validazione del permesso negli endpoint API, dati sensibili senza crittografia. Risolvere queste vulnerabilità prima del lancio è impegnativo, ma del tutto fattibile.

Questa guida copre i cinque livelli di sicurezza che ogni SaaS deve avere implementati prima di ricevere i primi utenti reali.

## Autenticazione: MFA, Session Management e Brute Force

L'autenticazione è la porta d'ingresso — e la maggior parte degli attacchi riusciti inizia qui. Implementare un'autenticazione sicura va oltre il "verificare email e password".

**MFA (Multi-Factor Authentication):**

Per i SaaS B2B, l'MFA non è opzionale — è atteso. I clienti aziendali richiedono frequentemente supporto a TOTP (Google Authenticator, Authy) come requisito di sicurezza. L'implementazione può essere realizzata con librerie come `otplib` (Node.js) in meno di un giorno.

```javascript
import { authenticator } from 'otplib';
import { toDataURL } from 'qrcode';

// Genera secret e QR Code per configurare l'MFA
async function setupMFA(userId: string) {
  const secret = authenticator.generateSecret();
  const otpauth = authenticator.keyuri(userEmail, 'MioSaaS', secret);
  const qrCodeUrl = await toDataURL(otpauth);

  // Salva il secret crittografato nel database (mai in chiaro)
  await db.user.update({
    where: { id: userId },
    data: { mfaSecret: encrypt(secret), mfaEnabled: false }, // attiva solo dopo verifica
  });

  return { qrCodeUrl, secret };
}

// Verifica il codice TOTP al login
function verifyMFAToken(secret: string, token: string): boolean {
  return authenticator.check(token, secret);
}
```

**Protezione contro il brute force:**

Ogni endpoint di login ha bisogno di rate limiting specifico per IP e per email. Senza di esso, gli strumenti automatizzati testano migliaia di password in pochi secondi.

```javascript
// Rate limiting nell'endpoint di login
import rateLimit from 'express-rate-limit';

const loginLimiter = rateLimit({
  windowMs: 15 * 60 * 1000, // finestra di 15 minuti
  max: 5,                    // massimo 5 tentativi per IP
  message: 'Troppi tentativi. Riprova tra 15 minuti.',
  standardHeaders: true,
  legacyHeaders: false,
});

app.post('/auth/login', loginLimiter, handleLogin);
```

**Session Management:**

- Token JWT con scadenza di 15-60 minuti per gli access token
- Refresh token con rotazione — ogni utilizzo invalida il precedente
- Revoca della sessione al cambio password o al rilevamento di attività sospette
- Cookie HttpOnly per archiviare i token nel browser (mai localStorage per access token sensibili)

## Autorizzazione: RBAC e Principio del Minimo Privilegio

L'autenticazione verifica *chi è* l'utente. L'autorizzazione verifica *cosa può fare*. Sono problemi diversi, e confonderli è una delle fonti più comuni di vulnerabilità nei SaaS.

**RBAC (Role-Based Access Control):**

Implementa ruoli chiari fin dall'inizio. Per la maggior parte dei SaaS B2B, tre ruoli sono sufficienti nell'MVP:

| Ruolo | Permessi tipici |
|-------|-----------------|
| Owner | Tutto, incluso billing ed eliminazione dell'organizzazione |
| Admin | Gestione membri, configurazioni, tutti i dati |
| Member | Crea e modifica i propri record, visualizza quelli del team |
| Viewer (opzionale) | Solo lettura |

**Verifica del permesso in TUTTI gli endpoint:**

L'errore più comune è verificare l'autenticazione (l'utente è loggato?) ma non l'autorizzazione (l'utente ha il permesso per questa specifica risorsa?). Un membro con accesso all'organizzazione A non deve mai riuscire a leggere o modificare i dati dell'organizzazione B — anche se autenticato.

```typescript
// Middleware di autorizzazione per risorsa
async function requireOrganizationAccess(
  req: Request,
  res: Response,
  next: NextFunction
) {
  const { organizationId } = req.params;
  const userId = req.user.id;

  const membership = await db.organizationMember.findUnique({
    where: { organizationId_userId: { organizationId, userId } },
  });

  if (!membership) {
    return res.status(403).json({ error: 'Accesso negato' });
  }

  req.membership = membership; // ruolo disponibile per l'handler
  next();
}
```

## Crittografia: Dati a Riposo e in Transito

**In transito:** HTTPS obbligatorio su tutti gli endpoint. Senza eccezioni. Configura HSTS (HTTP Strict Transport Security) per forzare HTTPS anche se qualcuno tenta di accedere via HTTP.

**A riposo:** Non tutti i dati richiedono crittografia nel database, ma i dati sensibili sì:

- Password: non archiviare mai la password in chiaro o con crittografia reversibile. Usa bcrypt, Argon2 o scrypt con salt
- Segreti MFA e API key: crittografia simmetrica con chiave gestita esternamente (AWS KMS, Vault, variabile d'ambiente)
- Dati personali sensibili (dati sanitari, finanziari): crittografia a livello di campo quando richiesto dal GDPR

```typescript
// Crittografia di campo con AES-256-GCM
import { createCipheriv, createDecipheriv, randomBytes } from 'crypto';

const ALGORITHM = 'aes-256-gcm';
const KEY = Buffer.from(process.env.ENCRYPTION_KEY!, 'hex'); // 32 bytes

export function encrypt(text: string): string {
  const iv = randomBytes(16);
  const cipher = createCipheriv(ALGORITHM, KEY, iv);
  const encrypted = Buffer.concat([cipher.update(text, 'utf8'), cipher.final()]);
  const tag = cipher.getAuthTag();
  return `${iv.toString('hex')}:${tag.toString('hex')}:${encrypted.toString('hex')}`;
}

export function decrypt(data: string): string {
  const [ivHex, tagHex, encryptedHex] = data.split(':');
  const decipher = createDecipheriv(ALGORITHM, KEY, Buffer.from(ivHex, 'hex'));
  decipher.setAuthTag(Buffer.from(tagHex, 'hex'));
  return decipher.update(Buffer.from(encryptedHex, 'hex')) + decipher.final('utf8');
}
```

## Rate Limiting, WAF e Header di Sicurezza

**Rate limiting su tutte le route pubbliche:**

Non solo sul login. Gli endpoint di creazione account, recupero password, invio inviti, qualsiasi route che possa essere abusata da automazione necessita di rate limiting.

**Header di sicurezza HTTP:**

Una configurazione completa di header di sicurezza si implementa in pochi minuti e blocca un'intera classe di attacchi (XSS, clickjacking, MIME sniffing):

```javascript
// next.config.js — header di sicurezza
const securityHeaders = [
  { key: 'X-DNS-Prefetch-Control', value: 'on' },
  { key: 'Strict-Transport-Security', value: 'max-age=63072000; includeSubDomains; preload' },
  { key: 'X-Frame-Options', value: 'SAMEORIGIN' },
  { key: 'X-Content-Type-Options', value: 'nosniff' },
  { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
  { key: 'Permissions-Policy', value: 'camera=(), microphone=(), geolocation=()' },
  {
    key: 'Content-Security-Policy',
    value: "default-src 'self'; script-src 'self' 'unsafe-eval' 'unsafe-inline'; ...",
  },
];
```

**Checklist di sicurezza pre-lancio:**

| Voce | Priorità |
|------|---------|
| HTTPS + HSTS su tutti gli endpoint | Obbligatorio |
| Rate limiting su login e route pubbliche | Obbligatorio |
| Password con bcrypt/Argon2 | Obbligatorio |
| Verifica autorizzazione su tutti gli endpoint | Obbligatorio |
| Header di sicurezza HTTP configurati | Obbligatorio |
| Variabili d'ambiente mai nel repository | Obbligatorio |
| MFA disponibile (non obbligatorio nell'MVP) | Consigliato |
| Crittografia dei campi sensibili | Consigliato |
| Log di audit di sicurezza | Consigliato |
| Scansione dipendenze (npm audit) | Consigliato |

## Conclusione

La sicurezza non è una funzionalità da aggiungere dopo — è uno strato che permea tutta l'architettura. Le voci obbligatorie di questa lista richiedono da due a quattro giorni per essere implementate correttamente in un nuovo SaaS. Sono giorni che valgono anni di reputazione protetta.

Il costo di rimediare a una falla di sicurezza dopo un incidente — notifica ai clienti, potenziale GDPR (Europa), lavoro tecnico d'emergenza — è di ordini di grandezza superiore al costo di implementare queste protezioni prima del lancio.

In **SystemForge**, la sicurezza è parte della checklist standard di ogni SaaS che consegniamo: auth con MFA, RBAC implementato, header configurati, crittografia dei campi sensibili e audit log pronti prima del primo deploy.
